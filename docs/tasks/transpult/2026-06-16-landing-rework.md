---
role: transpult
status: pending
priority: P0 (split, см. §10)
created: 2026-06-16
updated: 2026-06-17 (v2.5 — Marketing-финал §4.3, последний gate снят)
version: v2.5
owner: /marketing
deadline_copy: 2026-06-23 (копи — Hero/Features/FAQ/Footer)
deadline_lead_capture: TBD (после закрытия классов 1+2+3 qa-audit-2026-06-14)
---

# Лендинг: ребилд под ТЭД-нишу (compliance-боль вместо «работает сама»)

## Изменения v2.5 (Marketing-финал §4.3 + последний gate снят, 17.06)

Marketing провёл competitor-research по 3 брендам (1С:ТМС / СБИС TMS / Умная Логистика) через веб-сверку публичных прайсов июнь 2026:

- **1С:ТМС:** лицензионная модель, точная цена — по запросу (нет публичной), позиционирование под 100+ машин с проектным внедрением.
- **СБИС TMS (Saby):** 5 000–150 000 ₽/мес, часть экосистемы Saby — бриф §6.5 «от 18 000 ₽/мес» **устарел**.
- **Умная Логистика (carriers):** 2 999 ₽/мес (2 юзера) / 8 999 ₽/мес (8 юзеров) — флагман-биржа + TMS, бриф §6.5 не упоминал.

Источники: [ul.su/carriers/rates](https://ul.su/carriers/rates/), [saby.ru/tms](https://saby.ru/tms), [vc.ru/services/2113922](https://vc.ru/services/2113922-luchshie-tms-sistemy-transportom).

**Отстройка ТрансПульта** — не «дешевле всех» (Умная Логистика сопоставима), а специализация: compliance-готовность к ЭТрН-2026 + малый парк 2–30 машин + без интегратора. §4.3 финализирован под формат брифа §6.7 (5 предложений, нейтрально, дисклеймер «по публичным прайсам июнь 2026» в самом ответе).

**Что это значит для гейтов:** последний `⏳ Marketing §4.3` снят. **Все 4 publish-gate теперь ✅** (Jurist / Partner / QA / Marketing §4.3). Осталось одно условие до merge: Jurist-ревью упоминания брендов конкурентов (бриф §10 чек-лист «риск претензий от 1С / Тензор за упоминание»). Это короткое ревью на 5 предложений, не блокирует подготовку merge.

Файл-история этой задачи: [docs/tasks/marketing/2026-06-16-competitor-faq-finalize.md](../marketing/2026-06-16-competitor-faq-finalize.md) — `status: done`.

---

## Изменения v2.4 (TransPult факт-синк, 17.06)

Закрыты два висячих хвоста: ТЗ ссылался на gate-доки, которых физически не было. Теперь оба созданы:

- `/jurist` → `docs/legal/landing-claims-review-2026-06-16.md` — дословные формулировки §§1.1/2.1/2.2/2.6/4.1/4.4 (колонка B) + блок запретов. TransPult сверяет копи с этим файлом при имплементации.
- `/qa` → `docs/tasks/qa/2026-06-16-landing-factcheck.md` — и **добил Q3**: прогнал `drizzle-kit check` (✅) + сверил миграционный след events (0027 +organization_id / 0030 org-FK RESTRICT / 0039 per-org unique) со `schema.ts` — **дрейфа нет**, бриф-⚠️ устарел. «append-only» остаётся overclaim (на events нет DB-триггера запрета UPDATE/DELETE) — копи §2.4 «журнал событий» корректна.

Итог QA fact-check: **Q1 ✅** (Android-only) · **Q2 ❌** (RBAC sweep не закрыт — §2.4 без RBAC) · **Q3 ✅+⚠️** (дрейфа нет; «append-only» — overclaim, убран). Прежняя метка «Q3 ⚠️ pending» устарела.

Публикация-gate без изменений: единственный открытый блокер — **Marketing §4.3** (FAQ про конкурентов). Старт копи §§1–5 в feature-ветке — авторизация Partner/PM.

## Изменения v2.3 (QA fact-check + правка §2.4 overclaim, 16.06)

`/qa` прошёл 3 fact-check вопроса по брифу §13. Вердикт:

| # | Вопрос | Вердикт | Влияние на копи |
|---|---|---|---|
| Q1 | iOS только Android в проде? | ✅ Android-only (eas.json: preview/production — только Android; iOS — dev-симулятор) | §2.3 «только Android, iOS в дорожной карте» — достоверно. **Marketing берёт на заметку:** не писать «уже в Google Play» / «скачать в Google Play» — релиза в стор по факту ещё нет (version 0.1.0, submit.production пуст) |
| Q2 | RBAC sweep закрыт? | ❌ Нет (открытые deferred-пункты в remediation-tracker + находки code-audit-2026-06-14) | §2.4 — RBAC из копи убрать (уже сделано в v2). Возврат — после закрытия sweep'а |
| Q3 | events schema-drift закрыт? | ⚠️ Не подтверждён (drizzle-kit check pending — Bash под классификатором). Append-only на events — конвенция + restrict, **не DB-триггер** | §2.4 — формулировка v2 «append-only журнал» = **overclaim**, заменена на нейтральное «журнал событий». Возврат «append-only» — только после DB-триггера запрета UPDATE/DELETE на events |

**Marketing-правка по итогам fact-check:**
- §2.4 desc: «Все действия пишутся в **append-only** журнал…» → «Все действия пишутся в **журнал** для внутреннего аудита и разбора инцидентов…». Это закрывает Q3-overclaim до DB-enforcement.
- §2.3 — копи v2 уже корректна, дополнительной правки не требует. Запись в pre-condition для будущих итераций: не возвращать «уже в Google Play» в FAQ/CTA/footer до релиза.

**Q3 не блокирует копи и публикация-gate.** §2.4 теперь нейтральна по обоим сценариям drizzle-kit check. Если QA найдёт дрейф — копи уже безопасна. Если дрейф чист — Marketing может вернуть «append-only» в следующей итерации (бэклог), но только при наличии DB-триггера.

Источник: чат `/qa` 16.06.2026 + готовится `docs/tasks/qa/2026-06-16-landing-factcheck.md` как фактический вердикт (QA оформит, ссылка появится в §11 «Связи»). §8 строка `/qa` — закрыта в этой версии.

---

## Изменения v2.2 (Partner-апрув split, 16.06)

`/partner` закрыл строку §8 «split (A/B/C)» — утверждена **опция C**. Дословно:

> «C — split подтверждён. Копи (§§1-5) — пишется сейчас к 23.06, не трогает stop-gate. **Публикация копи — после закрытия QA fact-check (§8) + финала §4.3 от Marketing.** Lead-capture (§6) — после закрытия классов 1+2+3 qa-audit. На разрыв CTA — mailto.»

Важное уточнение Partner'а — **публикация-gate**, явное условие к §10. Код копи пишется к 23.06 (это про ресурс TransPult и Monopoly-окно), но **деплой в прод** возможен только после ✅ от QA по 3 fact-check вопросам (iOS / RBAC / events schema-drift) + ✅ финала §4.3 от Marketing. Иначе мы публикуем claims, фактическую истину которых ещё не подтвердил QA — и это снова риск недостоверной рекламы по 38-ФЗ, ровно тот, от которого Jurist только что чистил формулировки.

§10 обновлён, чтобы публикация-gate был явный, а не вычитывался по §8. §8 строка `/partner` — закрыта со ссылкой на §10.

---

## Изменения v2.1 (Jurist-апрув с правками, 16.06)

Jurist прошёл §1.1 H1, §4.1 FAQ, §2.1–§2.6 Features на ФАС-риск (38-ФЗ ст. 5). Вердикт:

1. **§1.1 H1 — ✅ апрув без правок.** «Без штрафов» во множественном числе, без статьи/суммы = общая референция к выгоде, не утверждение о санкции.
2. **§4.1 FAQ — ⚠️ убрать процессуальную стадию.** «Статья 11.14.3 КоАП — рассматривается ко 2-му чтению» — Jurist не подтвердил по sozd.duma.gov.ru, при этом ст. 11.14.3 уже существует с конкретными суммами в публичных источниках — формулировка про «2-е чтение» = недостоверная реклама. Заменено на безопасную формулировку без статьи и без стадии.
3. **§2.2 — ❌ БЛОКЕР исправлен:** «Приказ Минтранса №2200» — неверно. №2200 — это **ПП РФ от 21.12.2020 № 2200 «Правила перевозок грузов»**, не приказ Минтранса и не про путевые листы. Путевой лист регулируется **Приказом Минтранса от 28.09.2022 № 390**. Исправлено. См. [docs/legal/etrn-legal-basis.md §1](../../legal/etrn-legal-basis.md).
4. **§2.1, §4.4 — даты-обещания смягчены модальностью.** «До 1 сентября — продакшн поддержка СБИС/Калуга/Такском» / «продакшн-подключение готовим к 1 августа» — публичные обещания конкретной даты. Если не успеем — ретроспективно недостоверный claim + основание для жалобы в ФАС. Заменено на «планируем».
5. **§2.6 ИИ-копилот** — «Доступен в Pro и Business» → «Появится в Pro и Business» (статус Beta, до подтверждения QA-fact-check'ом фактического присутствия в тарифах).
6. **Граница апрува.** Достоверность claims по факту (Android offline, append-only журнал, Диадок-sandbox, копилот в Pro/Business) — за QA. Если QA-fact-check (§8 pre-condition) даст «iOS на самом деле в проде» или «копилота в Pro нет» — формулировки придётся править отдельно.

Источник: чат `/jurist` 16.06.2026 + готовится `docs/legal/landing-claims-review-2026-06-16.md` как чек-лист claims с галочками (Jurist оформит, ссылка появится в §11 «Связи»).

---

## Изменения v2 (PM-ревью 16.06)

ТЗ v1 содержало 3 блокера и 3 правки от PM. Все приняты:

1. **Lead-модель — Drizzle, НЕ Prisma** (ADR 0006). §6.4 переписан под `pgTable` в `apps/api/src/db/schema.ts` + миграция `00XX_leads.sql`.
2. **Бриф `landing-v2-brief.md` v1.1 НЕ устарел.** Это стратегический источник (ICP, voice, allowed-claims, SEO, метрики, A/B). Данное ТЗ — имплементация поверх. Конфликты разрешаются в пользу брифа. Раздел «Связи» обновлён.
3. **Claims-fix** (приведено в соответствие с разделом 4 брифа):
   - §2.1 «двусторонний обмен с 1С» → «экспорт документов в 1С (XML)» (бриф ⚠️ → ✅-формулировка)
   - §2.1 ЭДО-операторы → формулировка брифа: «Планируем поддержку Диадок, СБИС, Калуга-Астрал, Такском. Сейчас подключён Диадок-sandbox»
   - §1.5 STATS «14 дней до ЭТрН» → удалено, заменено на проверяемое
4. **H1.** Сняты 3 варианта v1 (мой «A — штраф» = ФАС-риск, повторение снятого калькулятора штрафа). Дефолт — **бриф-вариант A из §6.1** (срок+результат). **Бриф-вариант B** (страх штрафа) — только после Jurist-апрува, для ретаргетинга. §1.1 переписан.
5. **/admin/leads — в scope P0, не опционально.** §6.6 обновлён. Без админки лиды без статуса/UTM = слепая воронка.
6. **Split по stop-gate** (новый §10): копи к 23.06 / lead-capture после security-блока (классы 1+2+3 qa-audit-2026-06-14). На разрыв CTA «Запросить демо» → временная Tilda-форма (MVP-инструмент брифа v1.1 §14) или `mailto:` fallback.
7. **Deadline** разделён: `deadline_copy = 2026-06-23`, `deadline_lead_capture = TBD`.
8. **Стартовые pre-conditions** (§9): Jurist-апрув формулировок ⚠️-claims + Marketing-финал ответа FAQ про конкурентов + подтверждение от Founder опции split.

---

## Контекст

Маркетинг провёл свежий аудит `/landing` (16.06.2026). Главный вывод: лендинг построен как SaaS-страница инди-формата («ТрансПульт, в котором логистика работает сама», «Начать бесплатно»), а должен быть как ТЭД-материал для собственника / коммерческого директора, пришедшего из Telegram-постов про ЭТрН-2026 («электронные накладные», «1С-обмен», «демо под ваш парк»).

Это ТЗ — **имплементация** правок Hero / Features / FAQ / Footer + новый `/demo` flow. **Стратегические правила** (ICP, voice, allowed-claims раздел 4, SEO, метрики, A/B) — в [landing-v2-brief.md v1.1](../../marketing/landing-v2-brief.md). При любом конфликте — бриф старше.

## Scope этого ТЗ

**Копи-часть** (P0, к 23.06.2026):
1. Hero — переписать H1, подзаг, CTA, бейджи, stats.
2. Features — перетасовать порядок, ИИ-копилот сдвинуть/убрать.
3. AI-секция в `page.tsx` (между HowItWorks и Pricing) — удалить.
4. FAQ — добавить compliance-вопрос первым, перетасовать.
5. Footer — убрать личный Telegram владельца из основного блока.

**Lead-capture-часть** (P0, после закрытия классов 1+2+3 qa-audit-2026-06-14, deadline TBD):
6. Страница `/demo` — форма заявки.
7. Backend `POST /api/leads/demo-request` + Drizzle-модель `leads` + миграция.
8. Админка `/admin/leads` — список + статус + UTM-разбивка.

**На разрыв** (между п.1-5 и п.6-8): CTA «Запросить демо» ведёт на временную Tilda-форму (если инфра брифа MVP уже стоит) либо `mailto:sales@transpult.ru` с предзаполненным телом.

**НЕ входит** (отдельные задачи, поставлю позже):
- vs-таблица «1С:ТМС / СБИС / Умная Логистика / мы» — после маркетингового ресёрча конкурентов. Бриф v1.1 §6.5 даёт черновик, но требует juris-апрув упоминаний брендов.
- SEO-посадочные `/etrn-2026`, `/integracja-1c-tms`, `/shtraf-bumazhnaya-tn-2026` — отдельный план (бриф §11 фаза 2).
- Сегментация лендинга под малый / средний / крупный парк — большая работа после positioning.
- Калькулятор штрафа — снят брифом v1.1 (ФАС-риск, ст. 11.14.3 КоАП на стадии 2-го чтения). Возврат — после принятия закона + Jurist-апрува формулы.
- Новый мокап в Hero (ЭТрН-документ / окно 1С-обмена) — это к `/desing`, отдельная итерация.
- Переписать `HowItWorks` под путь покупки (демо → пилот → договор) — после согласования с PM, в этой итерации `HowItWorks` оставляем как есть.

---

## 1. Hero (`apps/web/src/app/landing/components/Hero.tsx`)

### 1.1. H1 + подзаг

Источник: **бриф v1.1 §6.1, вариант A (на старт)**. Дефолт без A/B-теста на запуске.

Сейчас:
```
ТрансПульт, в котором логистика работает сама
Заказы, рейсы, ЭТрН и биллинг в одном кабинете.
Готов к работе с 1 сентября 2026.
```

Заменить на:
```
H1:  ЭТрН с 1 сентября 2026 — без бумаги, без штрафов,
     без 1С на вашей стороне.

Sub: Электронные транспортные накладные через подключённого
     оператора ЭДО, путевые листы и осмотры в одном кабинете.
     Для парка от 2 до 30 машин. Готовимся к новому закону вместе.
```

Слово «штраф» в H1 ОК (множественное число, без конкретной статьи и суммы — это общая референция, не реклама). Без слова «штраф» — не получится передать compliance-боль, ради которой клиент пришёл.

**Бриф-вариант B** (страх штрафа, цитирует Минтранс №2200 и ставит «бумажная ТН — это штраф») — **только после Jurist-апрува** и только для ретаргетинга, не для холодного трафика. В этом ТЗ его не включаем.

### 1.2. CTA

Сейчас primary = «Начать бесплатно» → `/signup`, secondary = «Посмотреть демо» → `#demo` (якорь на мокап).

Заменить:
- **Primary**: «Подключиться бесплатно →» → `/signup` (формулировка брифа §6.1).
- **Secondary**: «Посмотреть демо за 3 минуты» → `/demo` (после готовности lead-capture) либо временная Tilda/`mailto` (на разрыв, §10).

Примечание: в брифе §6.1 «Подключиться бесплатно» — primary. Это **не** «Запросить демо», как я предложил в v1. Корректно: self-serve остаётся главным CTA для холодного трафика (для ИП-сегмента с парком 2-15 машин self-serve работает — собственник сам логист), демо — secondary для тех, кому надо «сначала посмотреть».

### 1.3. Бейдж сверху Hero

Сейчас: «Пилот-фаза · ранние пользователи».

Заменить на: «Готовимся к ЭТрН-2026 · ранний доступ». Фокус на причине, не на «у нас ещё ничего нет». Иконка пульсирующего dot'а остаётся.

### 1.4. Trust badges под CTA-кнопками (3 пункта)

Сейчас:
- «Серверы в РФ» — `ShieldCheck`
- «Бэкапы и аудит-лог из коробки» — `Activity`
- «Минтранс №2200» — `FileText`

**Убрать «Минтранс №2200» из бейджа** — у нас нет аккредитации оператора ЭТрН, есть только ссылка на нормативный акт. В Trust strip брифа §6.1 эта формулировка тоже есть, но с оговоркой «реквизит, не сертификат». Безопаснее не выносить в Hero — оставить упоминание Приказа №2200 в Features (§2.2) и в `/legal/etrn`.

Заменить на:
- «Серверы и ПДн в РФ» — `ShieldCheck`
- «Бэкапы и аудит-лог из коробки» — `Activity`
- «152-ФЗ + DPA из коробки» — `Lock` (новый импорт из `lucide-react`)

### 1.5. STATS-блок (3 числа внизу Hero)

Сейчас:
```
0 ₽    — Бесплатно до 5 машин
ЭДО    — Оператор Диадок (на подключении)
РФ     — Серверы и данные в России
```

Заменить на:
```
0 ₽       — Бесплатно до 5 машин
Android   — Приложение водителя, offline
РФ        — Серверы и данные в России
```

Middle stat «14 дней до первой ЭТрН» из v1 — **снят PM**, мы не контролируем сроки подключения оператора. Замена «Android offline» — проверяемое утверждение (бриф §4 ✅), снимает возражение «у меня водители без интернета», работает на конверсию.

Accent-цвет (`text-accent-300`) применить к «0 ₽» (как сейчас).

### 1.6. Подзаголовок под STATS

Сейчас: «Пилот-фаза. Текущий статус системы — на странице статус.»

Оставить, минимальная правка тона: «Сейчас идёт ранний доступ — открываем 50 новых компаний в месяц. Статус системы — на странице [статус](/status).»

### 1.7. Мокап в Hero (блок справа)

Сейчас — фейковая dispatcher-доска. По брифу §0 и §6.3 — это не главная боль клиента. Заменить, но **не в этой итерации** — это к `/desing` (новый мокап: ЭТрН-документ или окно обмена с 1С). На P0 оставить текущий мокап, поставить TODO-комментарий в коде.

`id="demo"` со скроллом к мокапу — больше не нужен (CTA secondary теперь ведёт на `/demo` либо Tilda/mailto). Удалить `id="demo"` с блока, удалить `<a href="#demo">` если останется.

---

## 2. Features (`apps/web/src/app/landing/components/Features.tsx`)

Сейчас порядок в массиве `FEATURES`:
1. ЭТрН-2026 готовность
2. ИИ-копилот (Beta)
3. Мобильное приложение водителя
4. Контроль температуры
5. Интеграции 1С и ЭДО
6. Безопасность и аудит

Переставить (приоритет под compliance-боль, бриф §6.3):
1. **ЭТрН и путевые листы** (был #1)
2. **Экспорт в 1С и ЭДО** (был #5) — поднять
3. **Мобильное приложение водителя (Android)** (был #3)
4. **Безопасность и аудит** (был #6) — поднять
5. **Контроль температуры** (был #4)
6. **ИИ-копилот** (был #2) — опустить в конец, **снять бейдж Beta** (бейдж останется только в отдельной секции — но эта секция удаляется, см. §3, значит бейдж снимаем вместе с переносом)

### 2.1. Поправить копи карточки «Экспорт в 1С и ЭДО»

Сейчас:
> «Экспорт в 1С, маркировка «Честный знак», ОСАГО-мониторинг. Оператор ЭДО — Диадок (на подключении), остальные в дорожной карте.»

Проблема v1: я предложил «двусторонний обмен с 1С» и «единый адаптер ЭДО без оговорок» — это **не соответствует реальности** и брифу §4. Возврат к формулировке брифа.

Заменить на:
> «Экспорт документов в 1С (XML) для бухгалтерии. Маркировка «Честный знак», ОСАГО-мониторинг. Сейчас подключаем Диадок-sandbox. К 1 сентября планируем продакшн-подключение и поддержку СБИС, Калуга-Астрал, Такском.»

Заголовок карточки: «Экспорт в 1С и подключение к ЭДО» (вместо «Интеграции 1С и ЭДО» — «интеграции» слишком сильное слово для одностороннего экспорта).

### 2.2. Поправить копи «ЭТрН и путевые листы»

Сейчас:
> Title: «Готовность к требованиям ЭТрН-2026»
> Desc: «ЭТрН (Минтранс №2200), путевые листы, тех- и медосмотры, журналы 152-ФЗ. Шаблоны и сроки уже зашиты.»

Проблема v1: «уже зашиты» обещает готовый продакшн, а у нас sandbox. Возврат к формулировке брифа §6.3.

Заменить на:
> Title: «ЭТрН и путевые листы»
> Desc: «Формируем XML транспортных накладных по форматам Минтранса (в рамках ПП РФ №2200 «Правила перевозок грузов»). Электронные путевые листы по составу Приказа Минтранса №390, предрейсовые тех- и медосмотры через мобильное приложение. Архив с подписями и штампами времени — в кабинете.»

⚠️ **Jurist-правка v2.1:** ранее в формулировке стояло «Приказ Минтранса №2200» — это была ошибка. №2200 = ПП РФ от 21.12.2020 «Правила перевозок грузов», не приказ Минтранса. Путевой лист регулируется Приказом Минтранса от 28.09.2022 №390. Финальная формулировка — после ревью `docs/legal/landing-claims-review-2026-06-16.md`. См. также [docs/legal/etrn-legal-basis.md](../../legal/etrn-legal-basis.md).

### 2.3. Карточка «Мобильное приложение водителя»

Сейчас:
> «Offline-режим (WatermelonDB), осмотры, чекпоинты, фотофиксация документов прямо в смене.»

Поправить — убрать «WatermelonDB» (внутренний термин, ИП-сегменту это шум). По брифу §4 явно: **iOS убрано до App Store URL + UAT, только Android**.

Заменить на:
> Title: «Приложение водителя (Android)»
> Desc: «Маршрут, чек-лист осмотра, фотофиксация документов, подпись приёмки. Работает offline — синхронизация при появлении связи. iOS — в дорожной карте.»

### 2.4. Карточка «Безопасность и аудит»

Сейчас:
> «Серверы в РФ, RBAC, журнал событий, шифрование TLS 1.2+, on-premise по запросу.»

Проблема: RBAC, журнал событий, TLS — по брифу §4 это сейчас **в ⚠️-зоне** (Опция 2 RBAC sweep не закрыта, events schema-drift). Возврат к формулировке брифа §6.3.

Заменить на:
> Title: «Серверы в РФ + журнал событий»
> Desc: «Все действия пишутся в журнал для внутреннего аудита и разбора инцидентов. Серверы и данные — в РФ, 152-ФЗ. On-premise (Docker Compose) — по запросу для Enterprise.»

(Убрали RBAC и TLS до закрытия Опции 2. ⚠️ **Jurist+QA-правка v2.3:** в v2 было «append-only журнал» — overclaim по QA fact-check Q3: на events нет DB-триггера запрета UPDATE/DELETE, неизменяемость держится на конвенции, не на enforcement. Возврат «append-only» — только после DB-триггера + подтверждения отсутствия schema-drift по drizzle-kit check.)

### 2.5. Карточка «Контроль температуры»

Сейчас текст ОК, минимально переформулировать без «нативно» (англицизм):

> Title: «Контроль температуры в пути»
> Desc: «Алерты при выходе за коридор, журнал нарушений с привязкой к рейсу и водителю. Логирование с мобильного приложения и термодатчиков (интеграция в разработке).»

### 2.6. Карточка «ИИ-копилот»

Понизить до #6, **снять бейдж Beta** в карточке (он останется в отдельной AI-секции — но та удаляется, §3). Текст переформулировать в духе брифа §4 ⚠️ («помогает заполнять документы, не принимает решений»):

> Title: «ИИ-копилот диспетчера (Beta)»
> Desc: «Подсказывает оптимальный маршрут, помогает заполнить документы по тексту или голосу. Не принимает решений за вас — оставляем финальный шаг человеку. Появится в Pro и Business.»

Бейдж `Beta` (`f.badge`) — **оставить** в карточке (это единственное оставшееся место упоминания, без него непонятно что копилот не финальная функция).

---

## 3. AI-секция в `page.tsx`

Сейчас в `apps/web/src/app/landing/page.tsx` строки 28–63 — полноразмерная секция «Бонус: ИИ-копилот диспетчера (beta)» с 4-мя буллетами.

**Удалить эту секцию полностью.** По брифу §6.3 ИИ выносится на отдельную подстраницу `/ai-copilot` (фаза 2). На главной — упоминание только в карточке Features (§2.6).

После удаления:
- Импорт `Sparkles` из `lucide-react` в `page.tsx` убрать.
- `<section id="ai" …>` удалить целиком (строки 28–63).
- Сразу после `<section id="how">` идёт `<section id="pricing">` — проверить, что переходы фонов (`bg-white` ↔ `bg-neutral-50`) остаются ритмичными.

---

## 4. FAQ (`apps/web/src/app/landing/components/FAQ.tsx`)

Структура и тон ответов — по брифу §6.7 (10 вопросов из реальных возражений ИП). Здесь — только дельта к текущему массиву.

### 4.1. Добавить вопрос №1 (новый, первый в массиве, открытый по умолчанию)

```ts
{
    q: 'Что будет с моими документами 1 сентября 2026?',
    a: 'С 1 сентября 2026 транспортная накладная в бумажном виде перестаёт работать для ряда категорий грузов — сначала продукты, маркированные товары, опасные грузы, дальше остальные. Грузоотправители обязаны принимать только ЭТрН через оператора ЭДО. Без подключения вы либо рискуете административной ответственностью за нарушение порядка работы с перевозочными документами, либо выпадаете из тендеров: грузоотправитель не возьмёт перевозчика без ЭДО. Конкретные составы и суммы штрафов за ЭТрН уточняются законодателем — следим за изменениями. Подключение оператора ЭДО занимает 5–14 дней — лучше начать заранее.',
},
```

⚠️ **Jurist-правка v2.1:** в v2 было «статья 11.14.3 КоАП — рассматривается ко 2-му чтению, точные суммы уточняем». Стадия законопроекта не подтверждена по sozd.duma.gov.ru, при этом ст. 11.14.3 уже существует с конкретными суммами в публичных источниках — формулировка про «2-е чтение» = недостоверная реклама по ст. 5 ч. 3 38-ФЗ. Убрано и номер статьи, и стадия. Если Marketing захочет вернуть номер ст. 11.14.3 в текст — стадию надо сверить по sozd.duma.gov.ru и указать точно как там. Иначе не называть.

### 4.2. Добавить вопрос про пилот (2-й в массиве)

```ts
{
    q: 'Можно ли запустить пилот без оплаты?',
    a: 'Да. На бесплатном тарифе доступны все базовые модули для парка до 5 машин — этого хватит, чтобы попробовать ЭТрН и путевые листы на одном-двух рейсах. Для парка от 6 машин — пилот 30 дней на тарифе Старт со скидкой 50% (2 490 ₽), потом решаете. Запрос — на sales@transpult.ru или через форму «Посмотреть демо».',
},
```

### 4.3. Добавить вопрос про сравнение с конкурентами (3-й в массиве)

```ts
{
    q: 'Чем отличаетесь от 1С:ТМС, СБИС TMS, Умной Логистики?',
    a: '1С:ТМС — лицензионная модель с проектным внедрением через интегратора, заточена под парк 100+ машин. СБИС TMS — часть экосистемы Saby (бухгалтерия, ЭДО, отчётность), TMS — один из её модулей. Умная Логистика — облачная биржа и TMS с маркетплейсом грузов, сильна в поиске рейсов. ТрансПульт — специализированный продукт для ИП и малого парка 2–30 машин, фокус на готовности к ЭТрН с 1 сентября 2026, экспорт в 1С, без интегратора и собственной IT-команды. Цены и функционал конкурентов — по публичным прайсам июнь 2026, могут отличаться по региону и условиям.',
},
```

✅ Финализировано Marketing 16.06 (v2.5). Источники веб-сверки: [ul.su/carriers/rates](https://ul.su/carriers/rates/), [saby.ru/tms](https://saby.ru/tms), [vc.ru обзор TMS-2026/2027](https://vc.ru/services/2113922-luchshie-tms-sistemy-transportom). Дисклеймер «по публичным прайсам июнь 2026» — в самом ответе. Бренды конкурентов упомянуты нейтрально, без «лучший/превосходит/уступает» — готово к Jurist-ревью на риск претензий от 1С / Тензор / Smart Logistics за упоминание брендов (бриф §10 чек-лист).

### 4.4. Текущий первый вопрос «Какие операторы ЭДО поддерживаются?»

Поправить ответ под allowed-claims-list брифа §4:

Сейчас:
> «Контур.Диадок, СБИС, Калуга Астрал, Такском. Подключение через единый адаптер — переключиться можно без перенастройки документов.»

Заменить на:
> «Планируем поддержку Диадок (Контур), СБИС (Тензор), Калуга-Астрал, Такском. Сейчас активно тестируем Диадок-sandbox — продакшн-подключение планируем к 1 августа 2026. Договоры в работе.»

Опустить этот вопрос на 4-ю позицию (после трёх новых выше).

### 4.5. `useState<number | null>(0)` оставить — открытым по умолчанию будет новый compliance-вопрос (4.1).

### 4.6. Остальные вопросы (текущие 2–12) — оставить, переформулировать в стиле брифа §6.7 (короткие предложения, без канцелярита) **в этой итерации не трогаем** — отдельная задача для Marketing, не должна задерживать TransPult.

---

## 5. Footer (`apps/web/src/app/landing/components/Footer.tsx`)

В блоке контактов (строки 49–69) сейчас три ссылки:
- `support@transpult.ru`
- `+7 919 324-65-82`
- `@BardinGD` — Telegram (личный аккаунт владельца)

**Убрать `@BardinGD`** из публичного футера лендинга. Возражение safe / B2B-trust: личный контакт владельца в публичном футере читается как «один человек пилит TMS». Если нужен Telegram-канал продукта (`@transpult` или аналог) — `/marketing` создаст и пришлёт правильный URL отдельной задачей. В `/contacts` (отдельная страница) — можно оставить под пометкой «лично с основателем».

Убрать также импорт `Send` из `lucide-react` в `Footer.tsx`, если он больше нигде в файле не нужен.

---

## 6. Lead-capture (страница `/demo` + API + админка) — **после security-блока**

⚠️ **По §10 split:** этот раздел НЕ начинается до закрытия классов 1+2+3 qa-audit-2026-06-14. На разрыв CTA Hero secondary ведёт на временный канал (Tilda / mailto, §10.3).

### 6.1. Страница `/demo`

Создать `apps/web/src/app/demo/page.tsx` — публичная (без auth), форма + краткое описание.

UX:
- Краткое описание сверху: «На демо за 30 минут покажем (1) как ваши перевозки оформляются как ЭТрН, (2) как выгружаются в 1С, (3) сколько стоит на вашем парке. Без обязательств.»
- Форма ниже.
- После submit: благодарность + «Ответим в течение рабочего дня. Если срочно — sales@transpult.ru».
- Минимальный header (логотип + минимум навигации), фокус на конверсии.

### 6.2. Поля формы

| Поле | Required | Тип | Примечание |
|---|---|---|---|
| Имя | да | text | min 2 |
| Должность | нет | text | для сегментации (собственник / коммерческий / IT) |
| Компания | да | text | min 2 |
| ИНН | нет | text | если есть — авто-подтянуть название через DaData (UX уже есть в onboarding) |
| Email | да | email | стандартная валидация |
| Телефон | да | tel | формат +7 |
| Размер парка | да | select | «1–5 / 6–30 / 31–100 / 100+» |
| Что важнее всего? | нет | multiselect | «ЭТрН / 1С-выгрузка / Биллинг / Контроль водителей / Мобила / Аналитика / Другое» |
| Комментарий | нет | textarea | свободный |
| Согласие на обработку ПДн | да | checkbox | ссылка на `/legal/personal-data` |

UTM (`utm_source/medium/campaign/content/term`, `referrer`) — забираются из query + document.referrer, сохраняются hidden fields. Схема трекинга — бриф §8.

### 6.3. Backend-эндпоинт

`POST /api/leads/demo-request` в `apps/api/src/modules/leads/` (новый модуль).

Логика:
1. Валидация (zod-схема).
2. Rate-limit по IP (1 заявка / 5 минут — анти-спам).
3. INSERT в `leads` через Drizzle.
4. Email на `sales@transpult.ru` через существующий SMTP (Unisender, уже wired по комментарию PM).
5. Telegram-уведомление в чат продаж — TODO для `/devops` (если бота нет — пока только email).
6. Ответ `{ ok: true, leadId }`.

### 6.4. Drizzle-модель `leads` (НЕ Prisma — ADR 0006)

Добавить в `apps/api/src/db/schema.ts` (точное имя файла уточнит TransPult; в проекте Drizzle pgTable):

```ts
import { pgTable, text, varchar, timestamp, uuid, pgEnum, jsonb, inet } from 'drizzle-orm/pg-core';

export const leadStatusEnum = pgEnum('lead_status', [
  'new',
  'contacted',
  'demo_scheduled',
  'pilot',
  'won',
  'lost',
]);

export const fleetSizeEnum = pgEnum('lead_fleet_size', [
  '1-5',
  '6-30',
  '31-100',
  '100+',
]);

export const leads = pgTable('leads', {
  id: uuid('id').primaryKey().defaultRandom(),
  name: text('name').notNull(),
  position: text('position'),
  company: text('company').notNull(),
  inn: varchar('inn', { length: 12 }),
  email: text('email').notNull(),
  phone: varchar('phone', { length: 32 }).notNull(),
  fleetSize: fleetSizeEnum('fleet_size').notNull(),
  interests: text('interests').array(),
  comment: text('comment'),
  source: text('source').notNull(), // '/demo' | '/contacts' | ...
  utmSource: text('utm_source'),
  utmMedium: text('utm_medium'),
  utmCampaign: text('utm_campaign'),
  utmContent: text('utm_content'),
  utmTerm: text('utm_term'),
  referrer: text('referrer'),
  ipAddress: inet('ip_address'),
  userAgent: text('user_agent'),
  status: leadStatusEnum('status').notNull().default('new'),
  assignedTo: uuid('assigned_to'), // → users.id, FK добавит TransPult по существующим conventions
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull().defaultNow(),
});
```

Миграция: `apps/api/src/db/migrations/00XX_leads.sql` (номер — следующий в очереди). TransPult: проверь `drizzle.config.ts` на способ генерации (drizzle-kit `generate` или ручная sql) — следуй существующей конвенции в проекте.

Если в проекте есть FK на `users.id` через uuid — выровнять тип `assignedTo`. Если базовый ID-тип в проекте `text/cuid` — переписать `uuid` соответственно.

### 6.5. Admin-просмотр лидов `/admin/leads` — **в scope P0** (правка PM)

⚠️ **Не опционально.** Без админки лиды без статуса/UTM = слепая воронка. Email уведомления теряются в Inbox без напоминания.

Минимальный функционал v1:
- Страница `apps/web/src/app/admin/leads/page.tsx` под существующим admin-layout.
- Доступ — `super` + `admin` (по существующему RBAC).
- Список: таблица колонок `created_at | name | company | fleet_size | source/utm | status | actions`.
- Фильтры: `status`, `utm_source`, диапазон дат (`created_at`).
- Действие на строке: смена `status` (dropdown), просмотр карточки лида (модалка с полным списком полей + комментарий).
- Пагинация: 50/стр.
- Export CSV — optional, по запросу `/marketing` после первого месяца.

Не делаем v1: bulk-actions, теги, e-mail-цепочки, slip-in/out статусов с историей. Это CRM-функционал — отдельная задача.

---

## 7. Acceptance criteria

### Копи-часть (к 23.06.2026)
1. ✅ Hero H1 = «ЭТрН с 1 сентября 2026 — без бумаги, без штрафов, без 1С на вашей стороне.», подзаг — по брифу §6.1 вариант A.
2. ✅ Primary CTA = «Подключиться бесплатно →» → `/signup`. Secondary = «Посмотреть демо за 3 минуты» → `/demo` (либо временный канал, §10).
3. ✅ Trust badges под CTA не содержат «Минтранс №2200»; STATS не содержит «Диадок (на подключении)» и не содержит «14 дней».
4. ✅ Features-карточки идут в новом порядке (ЭТрН → 1С/ЭДО → Мобила(Android) → Безопасность → Температура → ИИ-копилот), все 6 копий соответствуют формулировкам брифа §4/§6.3 (никаких «двусторонних обменов», «единых адаптеров», «уже зашитых шаблонов», «RBAC/TLS» в Hero/Features).
5. ✅ Секция `<section id="ai">` в `page.tsx` удалена, импорт `Sparkles` убран.
6. ✅ В FAQ первый вопрос — про последствия неподключения ЭТрН с 01.09, открыт по умолчанию. Добавлены вопросы про пилот, про сравнение с конкурентами. Старый вопрос про операторов ЭДО переформулирован под allowed-claims.
7. ✅ Footer не содержит ссылки на `@BardinGD`, импорт `Send` убран если не нужен.
8. ✅ Битых импортов нет, build проходит.
9. ✅ Мобильный viewport (375px): H1 не уезжает, кнопки в первом экране видны без скролла.
10. ✅ Lighthouse mobile > 85 на `/landing` (бриф §10 ставит >90, в этой итерации — порог поснижен на время переходного состояния мокапа).

### Lead-capture-часть (deadline TBD, после security)
11. ✅ Drizzle-модель `leads` в `schema.ts`, миграция применена, типы экспортированы для api-роута.
12. ✅ `POST /api/leads/demo-request` работает: zod-валидация, rate-limit, INSERT, email-уведомление приходит на `sales@transpult.ru`.
13. ✅ UTM сохраняются в БД, видны в `/admin/leads`.
14. ✅ `/admin/leads` доступен для `super/admin`, показывает список с фильтрами и сменой статуса.
15. ✅ E2E тест на сабмит `/demo` (валидные данные → 200 + запись в БД).
16. ✅ Unit-тесты на zod-схему запроса.
17. ✅ Странички `/demo` Lighthouse mobile > 85, форма работает на 375px.

---

## 8. Pre-conditions (стартовые блокеры — должны быть закрыты ДО старта работы)

| Кто | Что | Статус | Блокирует |
|---|---|---|---|
| `/partner` | Подтвердить опцию split (рекомендация PM/Marketing — C). | ✅ Утверждена **опция C** (16.06, v2.2). Код копи (§§1–5) — пишется к 23.06, не трогает stop-gate. **Публикация копи — после ✅ QA fact-check + ✅ Marketing §4.3** (см. §10 публикация-gate). Lead-capture (§6) — после закрытия классов 1+2+3 qa-audit. На разрыв CTA — `mailto:`. | Снят |
| `/marketing` (я) | Финализировать ответ на FAQ-вопрос про конкурентов (§4.3 — сейчас черновик из брифа §6.5). | TODO | §4.3 публикация |
| `/jurist` | Подтвердить, что формулировки §1.1 H1, §4.1 FAQ, §2.1–§2.6 Features соответствуют брифу §4 allowed-claims и не несут ФАС-риска. | ✅ Апрув с правками 16.06 (v2.1). §1.1 ОК без правок. §4.1 — убрана недостоверная стадия «2-е чтение» + номер ст. 11.14.3. §2.1, §4.4 — даты смягчены на «планируем». §2.6 — «доступен» → «появится». Граница: достоверность фактических claims (Android offline, копилот в Pro/Business, Диадок-sandbox) — за QA-fact-check. См. `docs/legal/landing-claims-review-2026-06-16.md` (готовит Jurist). | Снят |
| `/jurist` | Подтвердить статус упоминания НПА: №2200 в Hero (§1.4) — убрать (сделано); в Features (§2.2) — корректность типа акта и предмета. | ✅ Правка применена 16.06 (v2.1). §2.2 переписан: путевые листы — Приказ Минтранса №390; ПП РФ №2200 «Правила перевозок грузов» — в контексте ЭТрН, не путевого листа. Финальная формулировка — после ревью legal-review. | Снят |
| `/qa` | Fact-check 3 вопроса по брифу §13: (1) iOS, (2) RBAC sweep, (3) events schema-drift. | ✅ Снят, вердикт-файл создан (v2.4, 17.06). **Q1 ✅** Android-only по eas.json. **Q2 ❌** RBAC sweep не закрыт — §2.4 уже без RBAC в v2. **Q3 ✅+⚠️** drizzle-kit check ✅ + миграции events (0027/0030/0039) сходятся со schema.ts → **дрейфа нет**; «append-only» — overclaim (нет DB-триггера), §2.4 = «журнал событий». Граница: Marketing не возвращает «уже в Google Play» / «append-only» до DB-триггера и релиза в стор. Вердикт: `docs/tasks/qa/2026-06-16-landing-factcheck.md`. | Снят |
| `/transpult` | Подтвердить статус: классы 1+2+3 qa-audit-2026-06-14 — в работе/закрыты. От этого зависит срок lead-capture-части. | TODO | §6 старт |

---

## 9. Зависимости от других ролей (имплементационные)

| Кто | Что | Когда |
|---|---|---|
| `/desing` | Новый мокап в Hero (ЭТрН-документ или 1С-обмен) — отдельная итерация после P0 копи. | После P0 |
| `/devops` | Telegram-уведомление в чат продаж для §6.3 п.5. Подтвердить, что SMTP (Unisender) в проде wired для отправки на `sales@transpult.ru`. | Параллельно lead-capture |
| `/qa` | E2E на форму `/demo` + админка `/admin/leads`. Регрессия лендинга (мобильный viewport, конверсии). | После imp |

---

## 10. Split по stop-gate (новый раздел, по PM-ревью)

### 10.1. Контекст
TransPult сейчас в работе по классам 1+2+3 из qa-audit-2026-06-14 (деньги / безопасность-эксплуатация / corrective_upd) — это **stop-gate выше любых маркетинговых задач**. Бриф landing-v2 v1.1 §10 это явно фиксирует: publish v2 невозможен до закрытия security-блока.

20 часов работы по лендингу поверх незакрытого stop-gate к 23.06 — нереалистично, PM не подтверждает.

### 10.2. Раздел работы

**Сейчас (код к 2026-06-23, публикация — после gate)** — копи-часть (§§1–5):
- Только текст и порядок массивов в существующих компонентах.
- Не трогает БД, не добавляет endpoints, не нарушает stop-gate.
- Объём: ~6 часов работы TransPult.
- Готов к старту Monopoly-кампании после прохождения публикация-gate (см. §10.2.bis).

### 10.2.bis. Публикация-gate (правка Partner'а v2.2)

**Код копи можно писать сейчас. Публиковать копи в прод — нельзя**, пока не закрыты:

1. ✅ **QA fact-check 3 вопроса** — **СНЯТО, вердикт-файл создан** (v2.4, 17.06). Q1 ✅ Android-only, Q2 ❌ RBAC sweep не закрыт (копи §2.4 уже без RBAC), Q3 ✅ дрейфа нет (drizzle-kit check ✅ + миграции events сходятся со schema.ts) + ⚠️ «append-only» overclaim, заменён в копи на «журнал событий». Вердикт — `docs/tasks/qa/2026-06-16-landing-factcheck.md`. **Не блокирует gate.**
2. ✅ **Marketing §4.3** — **СНЯТО v2.5** (17.06). Финальный ответ FAQ про конкурентов написан на основе веб-сверки публичных прайсов июнь 2026. См. §4.3 ниже и `docs/tasks/marketing/2026-06-16-competitor-faq-finalize.md`. Перед merge — короткое Jurist-ревью на упоминание брендов (бриф §10), это не блокер подготовки feature-ветки.

Workflow для TransPult:
- Имплементация копи в `apps/web/src/app/landing/` — в feature-ветке (например, `feature/landing-rework`), **без merge в main**.
- PR на review для `/marketing` + `/jurist` — сверка финальных формулировок с `docs/legal/landing-claims-review-2026-06-16.md`.
- Merge в main + деплой в прод — **только после ✅ QA + ✅ Marketing §4.3**.

Если QA или §4.3 затягиваются — feature-ветка ждёт. Это нормально, копи готова заранее, чтобы публикация была мгновенной после закрытия gate, а не «теперь ещё неделю писать».

**После закрытия классов 1+2+3 qa-audit-2026-06-14** — lead-capture-часть (§6):
- Новая Drizzle-модель + миграция (требует продакшн-миграцию).
- Новый endpoint + страница + админка.
- Объём: ~14 часов работы TransPult.
- Точный deadline — выставит PM после фиксации дат закрытия security-блока.

### 10.3. CTA «Посмотреть демо» на разрыв

Пока lead-capture не готов, secondary CTA в Hero (§1.2) ведёт **не на `/demo`**, а на временный канал:

**Вариант A — Tilda-форма** (рекомендую, если инфра уже стоит):
- Бриф v1.1 §14 / change-log упоминает MVP-лендинг на Tilda с e-mail capture, запуск был запланирован на 01.06. Если он уже работает — переиспользовать форму как `https://transpult.tilda.ws/demo` (или аналог).
- UTM сохраняются как hidden fields в Tilda, переносим в нашу БД скриптом после готовности `/admin/leads`.
- Лиды в этот период идут в `sales@transpult.ru` + Tilda-таблица.

**Вариант B — `mailto:` fallback** (если Tilda не готова):
```
mailto:sales@transpult.ru
?subject=Демо ТрансПульт — заявка с лендинга
&body=Имя:%0AКомпания:%0AРазмер парка:%0AТелефон:%0AЧто важно (ЭТрН/1С/биллинг/другое):
```
- Минус: нет UTM, теряем аналитику источника. Принимаем эту потерю на 1–2 недели разрыва.
- Плюс: 5 минут на правку `href`, ноль зависимостей.

`/marketing` выберет конкретный вариант после ответа `/devops` по статусу Tilda. По умолчанию для TransPult: использовать **mailto** (это работает без зависимостей). Заменим на Tilda-URL отдельным мелким PR, если выберем Tilda.

### 10.4. После закрытия security-блока

Lead-capture (§6) ставится отдельной задачей или продолжением этой — на усмотрение PM. CTA в Hero меняется с `mailto`/Tilda на `/demo` одной строкой.

---

## 11. Связи

- **Бриф (стратегия, источник истины):** [docs/marketing/landing-v2-brief.md](../../marketing/landing-v2-brief.md) v1.1 от 2026-05-28. **НЕ устарел**, остаётся в силе. Это ТЗ — имплементация поверх. При конфликте — бриф старше.
- **Jurist claims-review:** [docs/legal/landing-claims-review-2026-06-16.md](../../legal/landing-claims-review-2026-06-16.md) — ✅ создан (v1.0). Чек-лист claims с дословными формулировками (колонка B). TransPult сверяет копи с этим документом при имплементации.
- **QA fact-check:** [docs/tasks/qa/2026-06-16-landing-factcheck.md](../qa/2026-06-16-landing-factcheck.md) — ✅ создан. Фактический вердикт по 3 вопросам (iOS / RBAC / events) с привязкой к коду. Источник правды по достоверности §2.3 и §2.4.
- **ЭТрН правовая база:** [docs/legal/etrn-legal-basis.md](../../legal/etrn-legal-basis.md) — справочник по НПА (ПП РФ №2200, Приказ Минтранса №390, ст. 11.14.3 КоАП и т.п.). Источник правды по нормативным ссылкам в копи.
- [docs/marketing/2026-05-26-monopoly-takeover.md](../../marketing/2026-05-26-monopoly-takeover.md) — beachhead-кампания, под которую копи готовится к 23.06.
- [docs/tasks/transpult/2026-05-23-code-inventory.md](2026-05-23-code-inventory.md) — модуль `leads` отсутствует в инвентаре, эта задача его создаёт.
- [docs/qa/code-audit-2026-06-14] (если файл есть) — классы 1+2+3 = stop-gate, выше lead-capture-части.
- [docs/architecture/ADR-0006-drizzle-vs-prisma.md] (если файл существует под этим именем) — обоснование Drizzle.

### Следующие задачи (поставлю отдельно):
- `2026-06-XX-competitor-research-vs-table` — vs-таблица 1С:ТМС / СБИС / Умной Логистики (для §4.3 FAQ + бриф §6.5)
- `2026-06-XX-seo-landing-etrn-2026` — SEO-посадочные под compliance-запросы (бриф §11 фаза 2)
- `2026-06-XX-tilda-form-handover` — миграция лидов из Tilda-таблицы в `/admin/leads` после готовности lead-capture (если выберем вариант 10.3.A)
- `2026-06-XX-segment-landings` — лендинги под малый / средний / крупный парк

---

## Summary (заполняется по завершении)

— TODO: `/transpult` после имплементации.
