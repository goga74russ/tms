-- ============================================================
-- 0054 — P2 (код-аудит 2026-06-14): нет DB-уникальности на drivers.user_id →
-- один user мог иметь несколько driver-записей → недетерминированный driver-RLS
-- (resolveDriverId возвращал произвольную). Один driver на user.
--
-- Данные: при существующих дублях user_id создание индекса упадёт — предварить
-- дедупом при необходимости. user_id NOT NULL, поэтому обычный unique-индекс.
-- BEGIN/COMMIT — атомарность.
-- ============================================================
BEGIN;

CREATE UNIQUE INDEX IF NOT EXISTS idx_drivers_user_unique ON drivers (user_id);

COMMIT;
